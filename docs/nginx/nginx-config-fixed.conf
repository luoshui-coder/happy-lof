# HTTP 服务器配置（80 端口）- 用于 Let's Encrypt 验证
server {
    listen 80;
    server_name luoshui.life;
    
    # 网站根目录
    root /www/wwwroot/happy-lof;
    index index.html index.htm default.htm default.html;
    
    # Let's Encrypt 证书验证目录
    location /.well-known/ {
        root /www/wwwroot/java_node_ssl;
        allow all;
    }
    
    # 禁止在证书验证目录放入敏感文件
    if ( $uri ~ "^/\.well-known/.*\.(php|jsp|py|js|css|lua|ts|go|zip|tar\.gz|rar|7z|sql|bak)$" ) {
        return 403;
    }
    
    # 其他请求反向代理到应用
    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header REMOTE-HOST $remote_addr;
        proxy_set_header X-Host $host:$server_port;
        proxy_set_header X-Scheme $scheme;
        proxy_connect_timeout 30s;
        proxy_read_timeout 86400s;
        proxy_send_timeout 30s;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
    
    # 禁止访问的文件或目录
    location ~ ^/(\.user.ini|\.htaccess|\.git|\.svn|\.project|LICENSE|README.md|package.json|package-lock.json|\.env) {
        return 404;
    }
    
    # 日志配置
    access_log /www/wwwlogs/happy-lof.log;
    error_log /www/wwwlogs/happy-lof.error.log;
}

# HTTPS 服务器配置（443 端口）- 证书申请成功后启用
# 注意：首次申请证书时，请注释掉此部分，等证书申请成功后再取消注释
# server {
#     listen 443 ssl http2;
#     server_name luoshui.life;
#     
#     # SSL 证书配置
#     ssl_certificate /www/server/panel/vhost/cert/luoshui.life/fullchain.pem;
#     ssl_certificate_key /www/server/panel/vhost/cert/luoshui.life/privkey.pem;
#     
#     # SSL 安全配置
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:HIGH:!aNULL:!MD5:!RC4:!DHE;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#     
#     # 网站根目录
#     root /www/wwwroot/happy-lof;
#     index index.html index.htm default.htm default.html;
#     
#     # 反向代理到应用
#     location / {
#         proxy_pass http://127.0.0.1:5000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_set_header REMOTE-HOST $remote_addr;
#         proxy_connect_timeout 30s;
#         proxy_read_timeout 86400s;
#         proxy_send_timeout 30s;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
#     
#     # 禁止访问的文件或目录
#     location ~ ^/(\.user.ini|\.htaccess|\.git|\.svn|\.project|LICENSE|README.md|package.json|package-lock.json|\.env) {
#         return 404;
#     }
#     
#     # 日志配置
#     access_log /www/wwwlogs/happy-lof.log;
#     error_log /www/wwwlogs/happy-lof.error.log;
# }
